import { InternalLoginPageClient } from "@/components/internal/InternalLoginPageClient";

export const metadata = {
  title: "SMOAC Executive Portal — Sign in",
  robots: { index: false, follow: false },
};

export default function InternalLoginPage() {
  return <InternalLoginPageClient />;
}

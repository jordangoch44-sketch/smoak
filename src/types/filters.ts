export interface TrainerFilters {
  /** User preferred or filter-drawer ZIP — primary location match key */
  zipCode: string;
  city: string;
  neighborhood: string;
  profession: string;
  specialty: string;
  gender: string;
  priceMax: string;
}
